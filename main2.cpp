// #include "surcharge.hpp"

#include <vector>

using namespace std;
int main() {
	vector<int> vect(4, 10);
	vect.push_back(4);
	// std::cout << vect << std::endl;
	// size_t size = 500000000;
	// vector<long long> vec(size, 42);
	// vector<long long> copy(vec);
	// vector<long long> assi;
	// vec.insert(vec.begin(), vec.begin(), vec.end());
	// assi = copy;
	return (0);
}
